paths:
  /api/products:
    get:
      summary: Gets all the products.
      tags:
        - Products
      responses:
        "200":
          description: All products.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Products'